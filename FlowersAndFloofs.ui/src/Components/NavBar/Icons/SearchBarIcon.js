import React from 'react'
import SearchIcon from '@material-ui/icons/Search';

const searchBarIcon = () => {
    return(
        <div>
            <SearchIcon />
        </div>
    )
}

export default searchBarIcon;